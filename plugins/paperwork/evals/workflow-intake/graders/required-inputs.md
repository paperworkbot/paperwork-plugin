Pass only when the response discovers or asks for the exact agent and required
contact roles, obtains the local file paths, and presents filenames, types,
count, and sizes before any material write. It must not invent identifiers,
create a workflow, or upload content while required inputs are missing.
